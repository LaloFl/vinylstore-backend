{
  "name": "vinyl-back",
  "type": "module",
  "version": "0.1.0",
  "description": "Backend for web app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Diego Flores",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.12"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
